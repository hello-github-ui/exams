---
title: 05、数据结构与算法(10题)
source: 各大公司面试题库/面试题库（368题）/05、数据结构与算法(10题).pdf
pages: 8
converted_at: 2026-06-22 22:29:13
---

# 05、数据结构与算法(10题)

1. 说⼀一下⼏几种常⻅见的排序算法和分别的复杂度
a. 快速排序：
i. 原理理：快速排序采⽤用的是⼀一种分治的思想,它先找⼀一个基准数（⼀一般选择第⼀一个值）,然后将⽐比这个基准数⼩小的数字
都放到它的左边,然后再递归调⽤用,分别对左右两边快速排序,直到每⼀一边只有⼀一个数字.整个排序就完成了了.
1.选定⼀一个合适的值（理理想情况中值最好，但实现中⼀一般使⽤用数组第⼀一个值）,称为“枢轴”(pivot)。
2.基于这个值，将数组分为两部分，较⼩小的分在左边，较⼤大的分在右边。
3.可以肯定，如此⼀一轮下来，这个枢轴的位置⼀一定在最终位置上。
4.对两个⼦子数组分别重复上述过程，直到每个数组只有⼀一个元素。
5.排序完成。
ii. 复杂度：O(n)
iii. 特点：快速排序是我们平常最常使⽤用的⼀一种排序算法,因为它速度快,效率⾼高,是最优秀的⼀一种排序算法.
b. 冒泡排序：
i. 原理理：冒泡排序其实就是逐⼀一⽐比较交换,进⾏行行⾥里里外两次循环,外层循环为遍历所有数字,逐个确定每个位置,⾥里里层循环
为确定了了位置后,遍历所有后⾯面没有确定位置的数字,与该位置的数字进⾏行行⽐比较,只要⽐比该位置的数字⼩小,就和该位置的
数字进⾏行行交换.
ii. 复杂度：O(n^2)，最佳时间复杂度为O(n)
iii. 特点：冒泡排序在我们实际开发中,使⽤用的还是⽐比较少的.它更更加适合数据规模⽐比较少的时候,因为它的效率是⽐比较
低的,但是优点是逻辑简单,容易易让我们记得.
c. 直接插⼊入排序：
i. 原理理：直接插⼊入排序是将从第⼆二个数字开始,逐个拿出来,插⼊入到之前排好序的数列列⾥里里.
ii. 复杂度：O(n^2)，最佳时间复杂度为O(n)
iii. 特点：
d. 直接选择排序：
i. 原理理：直接选择排序是从第⼀一个位置开始遍历位置,找到剩余未排序的数据⾥里里最⼩小的,找到最⼩小的后,再做交换
ii. 复杂度：O(n^2)
iii. 特点：和冒泡排序⼀一样,逻辑简单,但是效率不不⾼高,适合少量量的数据排序
2. ⽤用java写⼀一个冒泡排序算法
1
public class Test {
2
   public static void bubbleSort() {
3
      int a[] = {49, 38, 65, 97, 76, 13, 27, 49, 78, 34, 12, 64, 5, 4, 62, 99, 98, 54, 56, 17
4
      int temp;
5
      for (int i = 0; i < a.length - 1; i++) {
6
         for (int j = 0; j < a.length - 1 - i; j++) {
7
            if (a[j] > a[j + 1]) {
8
               temp = a[j];
9
               a[j] = a[j + 1];
10
               a[j + 1] = temp;
11
            }
12
         }
13
      }
14
      for (int i = 0; i < a.length; i++)
15
         System.out.println(a[i]);
16
   }
17
​
18
   public static void main(String[] args) {
19
      bubbleSort();
20
   }
21
}
3. 描述⼀一下链式存储结构
a. 线性结构的优点是可以实现随机读取，时间复杂度为O(1)，空间利利⽤用率⾼高，缺点是进⾏行行插⼊入和删除操作时⽐比较麻烦，
时间复杂度为O(n)，同时容量量受限制，需要事先确定容量量⼤大⼩小，容量量过⼤大，浪费空间资源，过⼩小不不能满⾜足使⽤用要求，会

---

产⽣生溢出问题。
b. 链式存储结构的优点主要是插⼊入和删除⾮非常简单，前提条件是知道操作位置，时间复杂度是O(1)，但如果不不知道操作
位置则要定位元素，时间复杂度为O(n)，没有容量量的限制，可以使⽤用过程中动态分配的分配内存空间，不不⽤用担⼼心溢出问
题，但是它并不不能实现随机读取，同时空间利利⽤用率不不⾼高。
4. 如何遍历⼀一颗⼆二叉树
a. 树节点：
1
class TreeNode {
2
    int val;
3
    //左⼦子树
4
    TreeNode left;
5
    //右⼦子树
6
    TreeNode right;
7
    //构造⽅方法
8
    TreeNode(int x) {
9
        val = x;
10
    }
11
}
 
b. 递归先序遍历：先输出节点的值，再递归遍历左右⼦子树。中序和后序的递归类似，改变根节点输出位置即可。
1
// 递归先序遍历
2
public static void recursionPreorderTraversal(TreeNode root) {
3
    if (root != null) {
4
        System.out.print(root.val + " ");
5
        recursionPreorderTraversal(root.left);
6
        recursionPreorderTraversal(root.right);
7
    }
8
}
9
//1 2 4 6 7 8 3 5
c. 递归中序遍历：过程和递归先序遍历类似
1
// 递归中序遍历
2
public static void recursionMiddleorderTraversal(TreeNode root) {
3
    if (root != null) {
4
        recursionMiddleorderTraversal(root.left);
5
        System.out.print(root.val + " ");
6
        recursionMiddleorderTraversal(root.right);
7
    }
8
}
9
//4 7 6 8 2 1 3 5
d. 递归后序遍历：
1
// 递归后序遍历

![05、数据结构与算法(10题) 第2页插图](../assets/images/05、数据结构与算法(10题)_p2_1_bf0a825c.png)

---

2
public static void recursionPostorderTraversal(TreeNode root) {
3
    if (root != null) {
4
        recursionPostorderTraversal(root.left);
5
        recursionPostorderTraversal(root.right);
6
        System.out.print(root.val + " ");
7
    }
8
}
 
5. 倒排⼀一个LinkedList
1
Collections.reverse(linkedList);
6. ⽤用java写⼀一个递归遍历⽬目录下⾯面的所有⽂文件（directory.listFiles()）
1
void listAll(File directory) {
2
    if (!(directory.exists() && directory.isDirectory())) {
3
        throw new RuntimeException("⽬目录不不存在");
4
    }
5
​
6
    File[] files = directory.listFiles();
7
​
8
    for (File file : files) {
9
        System.out.println(file.getPath() + file.getName());
10
        if (file.isDirectory()) {
11
            listAll(file);
12
        }
13
    }
14
}
7. ⼆二叉树与红⿊黑树：
1. ⼆二叉树：
a. 特性：
i. 左⼦子树上所有结点的值均⼩小于或等于它的根结点的值。
ii. 右⼦子树上所有结点的值均⼤大于或等于它的根结点的值。
iii. 左、右⼦子树也分别为⼆二叉排序树。
b. 图例例：
c. 查找：⼆二分查找（通过⼀一层⼀一层的⽐比较⼤大⼩小来查找位置）：如查找值为10的节点：9--13--11--10
d. 缺陷：插⼊入容易易变成线性形态，查找性能⼤大打折扣，这时需要引⼊入红⿊黑树来解决

![05、数据结构与算法(10题) 第3页插图](../assets/images/05、数据结构与算法(10题)_p3_1_034dcf89.jpeg)

---

2. 红⿊黑树：
a. 特点：是⼀一种⾃自平衡的⼆二叉查找树，除了了符合⼆二叉树的特点之外，还符合以下⼏几点：
i. 节点是红⾊色或⿊黑⾊色。
ii. 根节点是⿊黑⾊色。
iii. 每个叶⼦子节点都是⿊黑⾊色的空节点（NIL节点）。
iv. 每个红⾊色节点的两个⼦子节点都是⿊黑⾊色。(从每个叶⼦子到根的所有路路径上不不能有两个连续的红⾊色节点)
v. 从任⼀一节点到其每个叶⼦子的所有路路径都包含相同数⽬目的⿊黑⾊色节点。
这些规则保证了红⿊树的⾃平衡。
红⿊树从根到叶⼦的最长路径不会超过最短路径的2倍。
提⾼寻址效率。
b. 添加删除：通过⾃自旋来保证平衡
8. b-tree、b+tree多叉树：
https://www.cnblogs.com/nullzx/p/8729425.html
1. b-tree（⽂文件系统）：B树也称B-树,它是⼀一颗多路路平衡查找树。我们描述⼀一颗B树时需要指定它的阶数，阶数表示了了⼀一
个结点最多有多少个孩⼦子结点，⼀一般⽤用字⺟母m表示阶数。当m取2时，就是我们常⻅见的⼆二叉搜索树。
a. 定义：
1）每个结点最多有m-1个关键字。
2）根结点最少可以只有1个关键字。
3）⾮非根结点⾄至少有Math.ceil(m/2)-1个关键字。
4）每个结点中的关键字都按照从⼩小到⼤大的顺序排列列，每个关键字的左⼦子树中的所有关键字都⼩小于它，⽽而右⼦子树
中的所有关键字都⼤大于它。
5）所有叶⼦子结点都位于同⼀一层，或者说根结点到每个叶⼦子结点的⻓长度都相同。
b. 插⼊入数据，向兄弟节点借，兄弟节点不不够则向⽗父节点借；
2. b+tree（mysql索引）：
a. 定义：
1）B+树包含2种类型的结点：内部结点（也称索引结点）和叶⼦子结点。根结点本身即可以是内部结点，也可以

![05、数据结构与算法(10题) 第4页插图](../assets/images/05、数据结构与算法(10题)_p4_1_8ab35e7f.png)

![05、数据结构与算法(10题) 第4页插图](../assets/images/05、数据结构与算法(10题)_p4_2_b9f542ee.jpeg)

---

是叶⼦子结点。根结点的关键字个数最少可以只有1个。
2）B+树与B树最⼤大的不不同是内部结点不不保存数据，只⽤用于索引，所有数据（或者说记录）都保存在叶⼦子结点
中。
3）m阶B+树表示了了内部结点最多有m-1个关键字（或者说内部结点最多有m个⼦子树），阶数m同时限制了了叶⼦子
结点最多存储m-1个记录。
4）内部结点中的key都按照从⼩小到⼤大的顺序排列列，对于内部结点中的⼀一个key，左树中的所有key都⼩小于它，右
⼦子树中的key都⼤大于等于它。叶⼦子结点中的记录也按照key的⼤大⼩小排列列。
5）每个叶⼦子结点都存有相邻叶⼦子结点的指针，叶⼦子结点本身依关键字的⼤大⼩小⾃自⼩小⽽而⼤大顺序链接。
9. 谈谈数据结构，⽐比如TreeMap：
TreeMap实现了了红⿊黑树的结构。
10. 图的深度遍历和⼴广度遍历
1、深度优先遍历：
    深度优先遍历结果是： A B E F C D G H I
深度优先遍历尽可能优先往深层次进⾏行行搜索
2、⼴广度优先遍历：
⼴广度优先遍历结果是： A B C D E F G H I
⼴广度优先遍历按层次优先搜索最近的结点，⼀一层⼀一层往外搜索。
11. 介绍⼀一下红⿊黑树、⼆二叉平衡树
12.  说说java集合，每个集合下⾯面有哪些实现类，及其数据结构。
a. HashMap：
i. 概念：

![05、数据结构与算法(10题) 第5页插图](../assets/images/05、数据结构与算法(10题)_p5_1_e428bc3e.png)

![05、数据结构与算法(10题) 第5页插图](../assets/images/05、数据结构与算法(10题)_p5_2_d4e50376.png)

![05、数据结构与算法(10题) 第5页插图](../assets/images/05、数据结构与算法(10题)_p5_3_0bea7039.png)

---

1. 在HashMap内部，采⽤用了了数组+链表的形式来组织键值对Entry<Key,Value>（利利⽤用数组的查询快+链表的插⼊入
删除快）
2. HashMap在存储键值对Entry<Key,Value>的时候，会根据Key的hashcode值，以某种映射关系，决定应当将
这对键值对Entry<Key,Value>存储在HashMap中的什什么位置上
3. 在JDK1.7进⾏行行多线程put操作，之后遍历，直接死循环，CPU飙到100%，在JDK 1.8中进⾏行行多线程操作会出现
节点和value值丢失，为什什么JDK1.7与JDK1.8多线程操作会出现很⼤大不不同，是因为JDK 1.8的作者对resize⽅方法进
⾏行行了了优化不不会产⽣生链表闭环。
ii. 结构：
iii. HashMap扩容：

![05、数据结构与算法(10题) 第6页插图](../assets/images/05、数据结构与算法(10题)_p6_1_5db44669.jpeg)

![05、数据结构与算法(10题) 第6页插图](../assets/images/05、数据结构与算法(10题)_p6_2_c8194241.png)

---

1、很简单的计算：由于默认的加载因⼦是0.75 ，那么，此时map的阀值是 16*0.75 = 12，即添加第13 个键值对<Key,Value>的
时候，map的容量会扩充⼀倍。
2、确实如此，但是为了尽可能第减少桶中的Entry<Key,Value>链表的长度，以提⾼HashMap的存取性能，确定的这个经验
值。如果读者你对存取效率要求的不是太⾼，想省点空间的话，你可以new HashMap(int initialCapacity, ﬂoat loadFactor)构造⽅
法将这个因⼦设置得⼤⼀些也⽆妨。
1. 扩容步骤：
1
1.申请⼀一个新的、⼤大⼩小为当前容量量两倍的数组；
2
2.将旧数组的Entry[] table中的链表重新计算hash值，然后重新均匀地放置到新的扩充数组中；
3
3.释放旧的数组；
2. 为何扩容为原来的两倍（性能）：
a. 在HashMap通过键的哈希值进⾏行行定位桶位置的时候，调⽤用了了⼀一个indexFor(hash, table.length);⽅方法。
1
   /**
2
     * Returns index for hash code h.
3
     */
4
    static int indexFor(int h, int length) {
5
        return h & (length-1);
6
    }
b. 通过限制length是⼀一个2的幂数，h & (length-1)和h % length结果是⼀一致的。
c. 如果length是⼀一个2的幂的数，那么length-1就会变成⼀一个mask, 它会将hashcode低位取出来，
hashcode的低位实际就是余数，和取余操作相⽐比，与操作会将性能提升很多。
_.  
iv. put流程：
1
a.  获取这个Key的hashcode值，根据此值确定应该将这⼀一对键值对存放在哪⼀一个桶中，即确定要存放桶的索引；
2
b.  遍历所在桶中的Entry<Key,Value>链表，查找其中是否已经有了了以Key值为Key存储的Entry<Key,Value>对象，
3
c1. 若已存在，定位到对应的Entry<Key,Value>,其中的Value值更更新为新的Value值；返回旧值；
4
c2. 若不不存在，则根据键值对<Key,Value> 创建⼀一个新的Entry<Key,Value>对象，然后添加到这个桶的Entry<Key,Va
5
d.  当前的HashMap的⼤大⼩小(即Entry<key,Value>节点的数⽬目)是否超过了了阀值，若超过了了阀值(threshold)，
6
    则增⼤大HashMap的容量量(即Entry[] table 的⼤大⼩小)，并且重新组织内部各个Entry<Key,Value>排列列。
v. get流程：
1
a.  获取这个Key的hashcode值，根据此hashcode值决定应该从哪⼀一个桶中查找；
2
b.  遍历所在桶中的Entry<Key,Value>链表，查找其中是否已经有了了以Key值为Key存储的Entry<Key,Value>对象，
3
c1. 若已存在，定位到对应的Entry<Key,Value>,返回value
4
c2. 若不不存在，返回null；
 
`_.

![05、数据结构与算法(10题) 第7页插图](../assets/images/05、数据结构与算法(10题)_p7_1_de0d4762.png)