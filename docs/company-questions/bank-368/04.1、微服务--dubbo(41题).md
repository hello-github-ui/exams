---
title: 04.1、微服务--dubbo(41题)
source: 各大公司面试题库/面试题库（368题）/04.1、微服务--dubbo(41题).pdf
pages: 7
converted_at: 2026-06-22 22:29:13
---

# 04.1、微服务--dubbo(41题)

Dubbo是国内最出名的分布式服务框架，也是 Java 程序员必备的必会的框架之⼀一。
1、Dubbo是什什么？
Dubbo是阿⾥里里巴巴开源的基于 Java 的⾼高性能 RPC 分布式服务框架，现已成为 Apache 基⾦金金会孵化项⽬目。官
⽹网：http://dubbo.apache.org
2、为什什么要⽤用Dubbo？
因为是阿⾥里里开源项⽬目，国内很多互联⽹网公司都在⽤用，已经经过很多线上考验。内部使⽤用了了 Netty、Zookeeper，保证了了⾼高性能
⾼高可⽤用性。
使⽤用 Dubbo 可以将核⼼心业务抽取出来，作为独⽴立的服务，逐渐形成稳定的服务中⼼心，可⽤用于提⾼高业务复⽤用灵活扩展，使前端
应⽤用能更更快速的响应多变的市场需求。
下⾯面这张图可以很清楚的诠释，最重要的⼀一点是，分布式架构可以承受更更⼤大规模的并发流量量。
下⾯面是 Dubbo 的服务治理理图。
 
3、Dubbo 和 Spring Cloud 有什什么区别？
两个没关联，如果硬要说区别，有以下⼏几点。
1）通信⽅方式不不同
1、Dubbo 使⽤用的是 RPC 通信，⽽而 Spring Cloud 使⽤用的是 HTTP RESTFul ⽅方式。
2、dubbo由于是⼆二进制的传输，占⽤用带宽会更更少（基于netty等）；springCloud是http协议传输，带宽会⽐比较多，同时
使⽤用http协议（http+restful api）⼀一般会使⽤用JSON报⽂文，消耗会更更⼤大。
3、dubbo的开发难度较⼤大，原因是dubbo的jar包依赖（存在代码级别的强依赖）问题很多⼤大型⼯工程⽆无法解决；
springcloud的接⼝口协议约定⽐比较⾃自由且松散，需要有强有⼒力力的⾏行行政措施来限制接⼝口⽆无序升级。
4、dubbo的改进是通过dubbofilter，很多东⻄西没有，需要⾃自⼰己继承，如监控，如⽇日志，如限流，如追踪。
springcloud具有配置管理理、服务发现、断路路器器、智能路路由、微代理理、控制总线、⼀一次性token、全局锁、选主、分布式会
话和集群状态等，满⾜足了了构建微服务所需的所有解决⽅方案。
2）组成部分不不同

![04.1、微服务--dubbo(41题) 第1页插图](../assets/images/04.1、微服务--dubbo(41题)_p1_1_88fd6420.jpeg)

![04.1、微服务--dubbo(41题) 第1页插图](../assets/images/04.1、微服务--dubbo(41题)_p1_2_b8d721c5.jpeg)

---

3）
4、dubbo都⽀支持什什么协议，推荐⽤用哪种？
dubbo://（推荐）
rmi://
hessian://
http://
webservice://
thrift://
memcached://
redis://
rest://
5、Dubbo需要 Web 容器器吗？
不不需要，如果硬要⽤用 Web 容器器，只会增加复杂性，也浪费资源。
6、Dubbo内置了了哪⼏几种服务容器器？
Spring Container
Jetty Container
Log4j Container
Dubbo 的服务容器器只是⼀一个简单的 Main ⽅方法，并加载⼀一个简单的 Spring 容器器，⽤用于暴暴露露服务。
7、Dubbo⾥里里⾯面有哪⼏几种节点⻆角⾊色？
8、画⼀一画服务注册与发现的流程图

![04.1、微服务--dubbo(41题) 第2页插图](../assets/images/04.1、微服务--dubbo(41题)_p2_1_c954cb7e.png)

![04.1、微服务--dubbo(41题) 第2页插图](../assets/images/04.1、微服务--dubbo(41题)_p2_2_d4f0566a.jpeg)

---

9、Dubbo默认使⽤用什什么注册中⼼心，还有别的选择吗？
推荐使⽤用 Zookeeper 作为注册中⼼心，还有 Redis、Multicast、Simple 注册中⼼心，但不不推荐。 
redis⽅案需要服务器时间同步，且性能消耗过⼤。
10、Dubbo有哪⼏几种配置⽅方式？
1）Spring 配置⽅方式 
2）Java API 配置⽅方式
11、Dubbo 核⼼心的配置有哪些？
配置之间的关系⻅见下图。

![04.1、微服务--dubbo(41题) 第3页插图](../assets/images/04.1、微服务--dubbo(41题)_p3_1_d4be7804.jpeg)

![04.1、微服务--dubbo(41题) 第3页插图](../assets/images/04.1、微服务--dubbo(41题)_p3_2_106c4088.png)

---

12、在 Provider 上可以配置的 Consumer 端的属性有哪些？
1）timeout：⽅方法调⽤用超时
2）retries：失败重试次数，默认重试 2 次 
3）loadbalance：负载均衡算法，默认随机 
4）actives 消费者端，最⼤大并发调⽤用限制
13、Dubbo启动时如果依赖的服务不不可⽤用会怎样？
Dubbo 缺省会在启动时检查依赖的服务是否可⽤用，不不可⽤用时会抛出异常，阻⽌止 Spring 初始化完成，默认 check="true"，可
以通过 check="false" 关闭检查。
14、Dubbo推荐使⽤用什什么序列列化框架，你知道的还有哪些？
推荐使⽤用Hessian序列列化，还有Duddo、FastJson、Java⾃自带序列列化。
15、Dubbo默认使⽤用的是什什么通信框架，还有别的选择吗？
Dubbo 默认使⽤用 Netty 框架，也是推荐的选择，另外内容还集成有Mina、Grizzly。 
16、Dubbo有哪⼏几种集群容错⽅方案，默认是哪种？
17、Dubbo有哪⼏几种负载均衡策略略，默认是哪种？

![04.1、微服务--dubbo(41题) 第4页插图](../assets/images/04.1、微服务--dubbo(41题)_p4_1_edd37630.png)

![04.1、微服务--dubbo(41题) 第4页插图](../assets/images/04.1、微服务--dubbo(41题)_p4_2_5842e870.jpeg)

---

18、注册了了多个同⼀一样的服务，如果测试指定的某⼀一个服务呢？
可以配置环境点对点直连，绕过注册中⼼心，将以服务接⼝口为单位，忽略略注册中⼼心的提供者列列表。
19、Dubbo⽀支持服务多协议吗？
Dubbo 允许配置多协议，在不不同服务上⽀支持不不同协议或者同⼀一服务上同时⽀支持多种协议。
20、当⼀一个服务接⼝口有多种实现时怎么做？
当⼀一个接⼝口有多种实现时，可以⽤用 group 属性来分组，服务提供⽅方和消费⽅方都指定同⼀一个 group 即可。
21、服务上线怎么兼容旧版本？
可以⽤用版本号（version）过渡，多个不不同版本的服务注册到注册中⼼心，版本号不不同的服务相互间不不引⽤用。这个和服务分组的
概念有⼀一点类似。
22、Dubbo可以对结果进⾏行行缓存吗？
可以，Dubbo 提供了了声明式缓存，⽤用于加速热⻔门数据的访问速度，以减少⽤用户加缓存的⼯工作量量。
23、Dubbo服务之间的调⽤用是阻塞的吗？
默认是同步等待结果阻塞的，⽀支持异步调⽤用。
Dubbo 是基于 NIO 的⾮非阻塞实现并⾏行行调⽤用，客户端不不需要启动多线程即可完成并⾏行行调⽤用多个远程服务，相对多线程开销较
⼩小，异步调⽤用会返回⼀一个 Future 对象。 
异步调⽤用流程图如下。
24、Dubbo⽀支持分布式事务吗？
⽬目前暂时不不⽀支持，后续可能采⽤用基于 JTA/XA 规范实现，如以图所示。

![04.1、微服务--dubbo(41题) 第5页插图](../assets/images/04.1、微服务--dubbo(41题)_p5_1_b65a4784.png)

![04.1、微服务--dubbo(41题) 第5页插图](../assets/images/04.1、微服务--dubbo(41题)_p5_2_dc511a35.jpeg)

---

25、Dubbo telnet 命令能做什什么？
dubbo 通过 telnet 命令来进⾏行行服务治理理，具体使⽤用看这篇⽂文章《dubbo服务调试管理理实⽤用命令》。
1
telnet localhost 8090
26、Dubbo⽀支持服务降级吗？
Dubbo 2.2.0 以上版本⽀支持。
27、Dubbo如何优雅停机？
Dubbo 是通过 JDK 的 ShutdownHook 来完成优雅停机的，所以如果使⽤用 kill -9 PID 等强制关闭指令，是不不会执⾏行行优雅停机
的，只有通过 kill PID 时，才会执⾏行行。
28、服务提供者能实现失效踢出是什什么原理理？
服务失效踢出基于 Zookeeper 的临时节点原理理。 （服务机器器会在zk上注册⼀一个临时节点，服务失效则临时节点被删除）
29、如何解决服务调⽤用链过⻓长的问题？
Dubbo 可以使⽤用 Pinpoint 和 Apache Skywalking(Incubator) 实现分布式服务追踪，当然还有其他很多⽅方案。
30、服务读写推荐的容错策略略是怎样的？
读操作建议使⽤用 Failover 失败⾃自动切换，默认重试两次其他服务器器。
写操作建议使⽤用 Failfast 快速失败，发⼀一次调⽤用失败就⽴立即报错。
31、Dubbo必须依赖的包有哪些？
Dubbo 必须依赖 JDK，其他为可选。
32、Dubbo的管理理控制台能做什什么？
管理理控制台主要包含：路路由规则，动态配置，服务降级，访问控制，权重调整，负载均衡，等管理理功能。
33、说说 Dubbo 服务暴暴露露的过程。
Dubbo 会在 Spring 实例例化完 bean 之后，在刷新容器器最后⼀一步发布 ContextRefreshEvent 事件的时候，通知实现了了 
ApplicationListener 的 ServiceBean 类进⾏行行回调 onApplicationEvent 事件⽅方法，Dubbo 会在这个⽅方法中调⽤用 ServiceBean ⽗父类 
ServiceConfig 的 export ⽅方法，⽽而该⽅方法真正实现了了服务的（异步或者⾮非异步）发布。
34、Dubbo 停⽌止维护了了吗？
2014 年年开始停⽌止维护过⼏几年年，17 年年开始重新维护，并进⼊入了了 Apache 项⽬目。
35、Dubbo 和 Dubbox 有什什么区别？
Dubbox 是继 Dubbo 停⽌止维护后，当当⽹网基于 Dubbo 做的⼀一个扩展项⽬目，如加了了服务可 Restful 调⽤用，更更新了了开源组件等。
36、你还了了解别的分布式框架吗？
别的还有 Spring cloud、Facebook 的 Thrift、Twitter 的 Finagle 等。

![04.1、微服务--dubbo(41题) 第6页插图](../assets/images/04.1、微服务--dubbo(41题)_p6_1_d7905eb7.jpeg)

---

37、Dubbo 能集成 Spring Boot 吗？
可以的，项⽬目地址如下。
https://github.com/apache/incubator-dubbo-spring-boot-project
38、在使⽤用过程中都遇到了了些什什么问题？
单⼀一⻓长连接和NIO异步通讯，适合⼤大并发⼩小数据量量的服务调⽤用，以及消费者远⼤大于提供者。Dubbo 的设计⽬目的是为了了满⾜足⾼高
并发⼩小数据量量的 rpc 调⽤用，在⼤大数据量量下的性能表现并不不好，建议使⽤用 rmi 或 http 协议。
39、你读过 Dubbo 的源码吗？
要了了解 Dubbo 就必须看其源码，了了解其原理理，花点时间看下吧，⽹网上也有很多教程，后续有时间我也会在公众号上分享 
Dubbo 的源码。
http://songwie.com/attached/dubbo/dubbo2.0.pdf
40、你觉得⽤用 Dubbo 好还是 Spring Cloud 好？
扩展性的问题，没有好坏，只有适合不不适合，不不过我好像更更倾向于使⽤用 Dubbo, Spring Cloud 版本升级太快，组件更更新替换
太频繁，配置太繁琐，还有很多我觉得是没有 Dubbo 顺⼿手的地⽅方……
41、注册中⼼心挂了了，消费者还能调⽤用服务者吗？
1. 注册中⼼心对等集群，任意⼀一台宕掉后，会⾃自动切换到另⼀一台
2. 注册中⼼心全部宕掉，服务提供者和消费者仍可以通过本地缓存通讯
3. 服务提供者⽆无状态，任⼀一台 宕机后，不不影响使⽤用
4. 服务提供者全部宕机，服务消费者会⽆无法使⽤用，并⽆无限次重连等待服务者恢复